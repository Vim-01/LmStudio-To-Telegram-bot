[![Python](https://img.shields.io/badge/Python-3.10%2B-blue)](https://python.org)
[![LM Studio](https://img.shields.io/badge/LM_Studio-0.4%2B-orange)](https://lmstudio.ai)
[![Telegram Bot](https://img.shields.io/badge/Telegram-Bot-blue)](https://telegram.org)
[![License](https://img.shields.io/badge/License-AGPL--3.0-green)](LICENSE)

**Прослойка между LM Studio и Telegram** для создания self-hosted ассистента с поддержкой ролевых моделей (RP), стриминга ответов и автоматического управления моделями.

---

## ✨ Особенности

- **⚡ Стриминг в реальном времени** — сообщение обновляется по мере генерации (каждые 1.5 сек для защиты от лимитов Telegram)
- **🤖 Умное управление моделями** — автоматическая загрузка/выгрузка через Native API LM Studio
- **📝 Полная конфигурация через файлы** — секреты в `.env`, модели и промпты в `config.json`
- **💾 Мультисессии** — переключайтесь между чатами с разными контекстами
- **🎭 Кастомные RP сценарии** — задавайте дополнительные промпты для каждой сессии
- **👁 Поддержка изображений** — анализ через Vision модель
- **🧠 Показ «мыслей» модели** — поддержка тегов `<think>` (reasoning)
- **🔒 Безопасность** — доступ ограничен одному пользователю (`ADMIN_ID`)

---

## 🚀 Быстрый старт

### 1. Требования

| Компонент | Версия | Ссылка |
|-----------|--------|--------|
| **Python** | 3.10+ | [python.org](https://python.org) |
| **LM Studio** | 0.4.0+ | [lmstudio.ai](https://lmstudio.ai) |
| **Telegram** | Аккаунт | [telegram.org](https://telegram.org) |
| **Git** | Любой | [git-scm.com](https://git-scm.com) |

### 2. Установка

```bash
# Клонируем репозиторий
git clone https://github.com/Vim-01/LmStudio-To-Telegram-bot.git
cd LmStudio-To-Telegram-bot

# Создаём виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 3. Настройка

```bash
# Копируем шаблон конфигурации
cp .env.example .env

# Редактируем параметры
nano .env
```

**Обязательные параметры в `.env`:**

```ini
# === TELEGRAM ===
TELEGRAM_TOKEN=ваш_токен_бота_от_@BotFather
ADMIN_ID=ваш_user_id_из_@userinfobot

# === LM STUDIO ===
LM_STUDIO_URL=http://localhost:1234
LM_API_TOKEN=ваш-токен-LMStudio

# === SYSTEM ===
TIMEOUT=600
BROWSER_TTL=600
LOG_LEVEL=INFO

# === FEATURES ===
ENABLE_VISION=true
ENABLE_STREAMING=true
```


**Настройка моделей в `config.json`:**
- Отредактируйте `models` — укажите точные имена моделей как в LM Studio
- Отредактируйте `prompts` — системные промпты для каждого режима

### 4. Запуск

```bash
chmod +x run.sh
./run.sh
```

Скрипт автоматически:
- ✅ Запустит сервер LM Studio если он не работает
- ✅ Дождётся инициализации API (до 45 сек)
- ✅ Создаст/активирует venv
- ✅ Установит зависимости
- ✅ Запустит Telegram бота

---

## 🔒 Безопасность

- ✅ Доступ ограничен одним пользователем (`ADMIN_ID`)
- ✅ Все конфигурации хранятся локально
- ✅ Нет отправки данных на внешние серверы
- ✅ `.env` и `sessions.json` в `.gitignore` — токены не попадут в репозиторий

> ⚠️ **Важно:** Никогда не коммитьте `.env` и `sessions.json` в Git!

---

## ⚠️ Важные примечания

1. **Длина контекста** — Настройте её в интерфейсе LM Studio.
2. **Стриминг** — обновляет сообщение каждые 1.5 секунды для защиты от `429 Too Many Requests`.
3. **Модели** — убедитесь что модели скачаны в LM Studio и имена в `config.json` совпадают.
4. **Python 3.14+** — проект протестирован на Python 3.14 (Arch Linux). Для старых версий может потребоваться `python-telegram-bot==21.6`.

---

## 🛠️ Интеграция с веб-поиском (опционально)

Для неекоторых режимов можно подключить локальный поиск через MCP:

1. Склонируйте [web-search-mcp](https://github.com/lmstudio-ai/web-search-mcp)
2. Настройте `~/.cache/lm-studio/mcp.json`:

```json
{
  "mcpServers": {
    "web-search": {
      "command": "node",
      "args": ["/полный/путь/web-search-mcp/dist/index.js"],
      "env": {
        "MAX_BROWSERS": "1",
        "BROWSER_HEADLESS": "true",
        "DEFAULT_TIMEOUT": "20000"
      }
    }
  }
}
```

3. Перезапустите LM Studio

---

## ❓ Частые проблемы

| Проблема | Решение |
|----------|---------|
| `Модель не загружена` | Убедитесь что модель скачана в LM Studio и имя в `config.json` совпадает точно |
| `429 Too Many Requests` | Стриминг автоматически ограничивает обновления (1 раз в 1.5 сек) |
| `Connection refused` | Запустите `./run.sh` — он сам поднимет сервер LM Studio |
| `Python 3.14 ошибка asyncio` | Убедитесь что `python-telegram-bot==21.10+` |
| Бот не отвечает на изображения | Проверьте что `VISION_ID` задан и модель загружена |
| `ModuleNotFoundError` | Запустите `pip install -r requirements.txt` внутри venv |

---

## 📜 Лицензия

**GNU Affero General Public License v3.0 (AGPL-3.0)**

Этот проект распространяется под лицензией AGPL-3.0.
Полный текст лицензии см. в файле [LICENSE](LICENSE).

---

## 🤝 Вклад в проект

Приветствуются:
- 🐛 Отчёты об ошибках
- 💡 Предложения по улучшению
- 🔧 Pull Request'ы с исправлениями
- 📖 Улучшение документации

Перед отправкой PR убедитесь что:
1. Код проходит `python -m py_compile bot.py`
2. Все секреты удалены из коммитов
3. Изменения протестированы локально

---

## 📬 Контакты

- **GitHub Issues**: [Сообщить об ошибке](https://github.com/Vim-01/LmStudio-To-Telegram-bot/issues)

---

## 🙏 Благодарности

- [LM Studio](https://lmstudio.ai) — за отличный локальный LLM-сервер
- [python-telegram-bot](https://python-telegram-bot.org) — за надёжную библиотеку для Telegram
- [httpx](https://www.python-httpx.org) — за асинхронный HTTP-клиент
- Всем контрибьюторам и пользователям проекта

---

```
